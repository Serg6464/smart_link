#export BOOST_VERSION=1.83.0
docker build -f otus.Dockerfile --tag=otus .
docker run -d -it --rm -p 8080:8080 -v $(pwd)/apps:/apps --name otus otus:latest ./httpgateway
docker exec -it otus ./httpredirector
#export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/lib/
